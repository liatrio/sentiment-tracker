# tests/test_app.py
import pytest  # noqa: F401


def test_example():
    """A simple example test to ensure pytest is set up correctly."""
    assert 1 == 1


# We can add more meaningful tests later, e.g., importing from src.app
# and testing some of its functionality.
# For now, this confirms pytest runs.
