# Slack Credentials
# Replace with your actual tokens from the Slack API dashboard
DEFAULT_SESSION_MINUTES=5 # Default session duration in minutes if not specified in /gather-feedback command
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token

# Additional configuration (optional)
# SLACK_LOG_LEVEL=DEBUG
