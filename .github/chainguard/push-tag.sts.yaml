issuer: https://token.actions.githubusercontent.com
subject_pattern: repo:liatrio/sentiment-tracker:ref:refs/heads/main
claim_pattern:
  workflow_ref: liatrio/sentiment-tracker/.github/workflows/semantic-release.yaml@.* # Still not sure what needs to come after the actual workflow name but this works

permissions:
  contents: write
